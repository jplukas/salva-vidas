%p
  Olá, #{@email}!
- if @resource.try(:unconfirmed_email?)
  %p
    Estamos lhe contatando para notificá-lo de que seu e-mail está sendo mudado para #{@resource.unconfirmed_email}.
- else
  %p
    Estamos lhe contatando para notificá-lo de que seu e-mail mudou para #{@resource.email}.
